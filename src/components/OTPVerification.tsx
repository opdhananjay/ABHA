const OTPVerification = ({ onClose }: { onClose: () => void }) => {
    return (
        <div>
            otp verification

            <button onClick={onClose}>Close</button>

        </div>
    )
}

export default OTPVerification;